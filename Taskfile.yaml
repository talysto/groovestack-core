# https://taskfile.dev

version: "3"

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - echo "{{.GREETING}}"
    silent: true

  tidy:
    cmds:
      - rubocop -A **/*.rb **/*.gemspec **/Gemfile
      - npx prettier --loglevel warn -w .
